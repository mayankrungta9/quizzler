import { ButtonControlDirective } from './button-control.directive';

describe('ButtonControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonControlDirective();
    expect(directive).toBeTruthy();
  });
});
